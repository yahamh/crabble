export interface GameHistory {
    playerName: string;
    opponentPlayerName: string;
    mode: string;
    date: string;
    finalScore: number;
    oponnentFinalScore: number;
    duration: string;
    abandoned: string;
}
